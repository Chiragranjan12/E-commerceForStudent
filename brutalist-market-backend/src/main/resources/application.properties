# Server port
server.port=8080

# Database Configuration (PostgreSQL)
spring.datasource.url=jdbc:postgresql://localhost:5432/brutalistmarket
spring.datasource.username=postgres
spring.datasource.password=Chir@gMishr@12
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration (Secret length must be at least 256 bits/32 characters)
jwt.secret=BrutalistMarketSuperSecretKeyForJwtAuthenticationWhichIsVeryLongAndSecure
jwt.expirationMs=86400000

# Logging
logging.level.org.springframework.security=DEBUG
